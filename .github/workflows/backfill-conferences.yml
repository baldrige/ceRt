name: Backfill conference reports

# One-off (manual) full-term fetch that renders a whole term's conference reports
# and merges them into the conferences/ archive on gh-pages. Use it to backfill a
# term the static archive doesn't cover yet (e.g. OT2025). Large and slow --
# thousands of dockets -- so it runs on a clean runner IP, not locally.

on:
  workflow_dispatch:
    inputs:
      terms:
        description: "Docket years to fetch and combine, comma-separated (e.g. 24,25)"
        required: true
        default: "24,25"
      min_conf_date:
        description: "Only render conferences on/after this date (YYYY-MM-DD)"
        required: true
        default: "2025-08-01"

permissions:
  contents: write

concurrency:
  group: conference-backfill
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    steps:
      - name: Checkout source (main)
        uses: actions/checkout@v4

      - name: Checkout published site (gh-pages) into ./site
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: site

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libpoppler-cpp-dev libtesseract-dev tesseract-ocr libcurl4-openssl-dev

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Install R packages (cached)
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          packages: |
            any::gt
            any::gtExtras
            any::tidyverse
            any::httr2
            any::jsonlite
            any::pdftools
            any::htmltools

      - name: Build conference reports (terms ${{ inputs.terms }})
        env:
          SITE_DIR: site
          TERM_YEARS: ${{ inputs.terms }}
          MIN_CONF_DATE: ${{ inputs.min_conf_date }}
        run: Rscript .github/scripts/build_conferences.R

      - name: Commit and publish
        working-directory: site
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to publish."
          else
            git commit -m "Backfill conference reports (terms ${{ inputs.terms }})"
            git push origin gh-pages
          fi
